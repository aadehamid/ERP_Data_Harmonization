# LSC Enterprise Data Harmonization — Python development dependencies
#
# Core stack from docs/synthetic-data-generation-strategy.md (§10.1):
#   Python + SQL, DuckDB/Polars, PyArrow/Parquet, Faker synthetic values,
#   Pydantic/JSON Schema data contracts, RDF/SKOS/SHACL/PROV-O semantic assets,
#   YAML/JSON mapping registry, Typer CLI, pytest/ruff/mypy dev tooling.
#
# Pins are compatible with Python 3.12. Regenerate with:
#   . .venv/bin/activate && pip install -U <pkg> && pip freeze > requirements.lock

# --- Data generation / transformation / storage ---
duckdb==1.4.1
polars==1.34.0
pyarrow==21.0.0
pandas==2.3.3

# --- Synthetic values ---
faker==38.2.0

# --- Data contracts / validation ---
pydantic==2.12.4
jsonschema==4.25.1
pandera==0.27.0

# --- Semantic assets: RDF / Turtle / SKOS / SHACL / PROV-O ---
rdflib==7.2.1
pyshacl==0.30.1

# --- Config / manifests / CLI ---
pyyaml==6.0.3
typer==0.20.0
rich==14.2.0

# --- Dev tooling ---
pytest==8.4.2
ruff==0.14.4
mypy==1.18.2
